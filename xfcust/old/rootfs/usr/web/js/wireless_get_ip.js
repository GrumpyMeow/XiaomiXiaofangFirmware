/*common function ,I will move it later*/
/*common function ,I will move it later*/

function initial_language() {
	$("#ip").text(ip); 		
	$("#header").text(WL_4IPtitle);
	$("#info").text(WL_4note);  	  		     
}
function initial_events() {
}
//document ready
$(function () {
    initial_language();
	initial_events();
});